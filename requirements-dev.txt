# Adds test (pytest, httpx) and lint (ruff, mypy) tooling on top of requirements.txt.
# Regenerate after changing pyproject.toml's test/lint extras:
#   pip install ".[test,lint]" && pip freeze | grep -v '^integra' | sort > /tmp/full.txt
#   comm -13 requirements.txt /tmp/full.txt   # then merge new lines in below

-r requirements.txt

Pygments==2.20.0
ast_serialize==0.6.0
botocore-stubs==1.43.14
certifi==2026.7.22
httpcore==1.0.9
httpx==0.28.1
iniconfig==2.3.0
librt==0.13.0
mypy==2.3.0
mypy_extensions==1.1.0
packaging==26.2
pathspec==1.1.1
pluggy==1.6.0
pytest-asyncio==1.4.0
pytest==9.1.1
ruff==0.16.1
types-aioboto3==15.5.0
types-aiobotocore==3.9.0
types-awscrt==0.34.1
types-s3transfer==0.16.0
